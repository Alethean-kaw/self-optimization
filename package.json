{
  "name": "self-optimization",
  "version": "5.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node ./tools/build.mjs",
    "test": "vitest run --dir tests --pool threads --poolOptions.threads.singleThread --no-file-parallelism --configLoader runner"
  },
  "devDependencies": {
    "@types/node": "^24.6.0",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
